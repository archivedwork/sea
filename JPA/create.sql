CREATE TABLE INVENTORY (NAME VARCHAR(255) NOT NULL, PRICE FLOAT, QUANTITY INTEGER, PRIMARY KEY (NAME))
CREATE TABLE BOOK (ID INTEGER NOT NULL, AUTHOR VARCHAR(255), PAGES INTEGER, STOCKSTATUS VARCHAR(255), TITLE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PRODUCT (ID BIGINT NOT NULL, LASTMODIFIED TIME, NAME VARCHAR(100), TYPE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
